import React from "react";

const Nav = () => {

    // Display components
    return (
        <div>
            <div className="nav-bg"/>
            <div className="nav">
                <span>Habit Tracker</span>
                <span>Profile</span>
            </div>
        </div>
    );
};

export default Nav;
